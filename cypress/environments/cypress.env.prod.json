{
  "_comment": "Production environment config. Replace placeholder values before use. Never commit real production credentials — use CI secrets instead.",
  "baseUrl": "https://your-app.com",
  "authUrl": "/api/auth/login",
  "sessionValidateUrl": "/api/v1/me"
}
